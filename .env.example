# App server port
PORT=3000

# Runtime mode used by run.sh
NODE_ENV=development

# SQLite database file path
DATABASE_PATH=./data/app.db

# Auth session lifetime in milliseconds
AUTH_SESSION_TTL_MS=604800000
